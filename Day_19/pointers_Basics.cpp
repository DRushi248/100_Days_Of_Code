#include<iostream>
using namespace std;

int main() {

    /*Basics:
    int a = 5;
    cout << a << endl;
    cout << "address of a: " << &a << endl;


    //pointer creation
    int* ptr = &a;
    //accessing  value stored at address stored in ptr
    cout << ptr << endl; //ptr wale dabbe me kya hai(address of a)
    cout << "Accessing: " << *ptr << endl; //ptr me jo address hai wo address pe kya value stored hai
    cout << &ptr << endl; //ptr wale dabbe ka address kya hai
    */


    /*
    // int* ptr; //Bad Practice
    int*  ptr = 0; //solution to bad practice
    cout << *ptr << endl;
    */

   /*
    int a = 5;
    int* p = &a;

    p = p + 1;
    cout << *p ;
    */

    /*
   int arr[5] = {1,2,3,4,5};
   arr = arr + 1;
   */


    /*char ch = 'a';
    char *cptr = &ch;
    cout << cptr;
    */

   /*
   char ch[10] = "a";
   char *cptr = ch;
   cout << cptr;
   */

  /*
  char ch[10] = "babbar";
  char* cptr = &ch[0];
  cout << &ch;
  */

    return 0;
}